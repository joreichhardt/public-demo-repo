# Kapitel 1

Ein Buchprojekt ist mehr als ein Text. Es ist eine Abfolge von Entscheidungen,
Dateien und überprüfbaren Änderungen.

Markdown hält den Text lesbar. Git hält die Historie sichtbar. Ein Agent kann
Ordner anlegen, Dateien prüfen und Exportschritte vorbereiten.

